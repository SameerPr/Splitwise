package model

import (
	"testing"
)

func TestNewExpense(t *testing.T) {
	//id, name, email := 1, "Test", "dummy@test.com"
	//user := NewUser(id, name, email)
	//e_id, amount, paidBy := 1, 200.0, user

}
